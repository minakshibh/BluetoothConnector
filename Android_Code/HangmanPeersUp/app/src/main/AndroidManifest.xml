<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hangmanpeersup">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="com.example.hangmanpeersup.ParseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".BluetoothActivity"></activity>
        <activity android:name=".RegisterActivity"></activity>
        <activity android:name=".fg"></activity>
        <activity android:name="com.example.hangmanpeersup.bluetoothchat.BluetoothChatFragment"></activity>
        <activity android:name="com.example.hangmanpeersup.bluetoothchat.DeviceListActivity"></activity>
        <activity android:name=".SelectedGameActivity"></activity>
        <activity android:name=".ForgetpasswordActivity"></activity>
        <activity android:name="com.example.hangmanpeersup.bluetoothchat.GameProgressActivity"></activity>
    </application>

</manifest>
